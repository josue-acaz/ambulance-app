interface TabContentProps {
    index: number;
    selected: number;
    padding?: number | string;
};

interface TabContentViewProps {
    padding?: number | string;
};

export type {
    TabContentProps,
    TabContentViewProps,
};