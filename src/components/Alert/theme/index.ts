import { colors } from "../../../design/colors";

export const theme_colors = {
    default: {
        main: colors.PRIMARY,
        text: colors.BLACK,
    },
    success: {
        main: colors.SUCCESS,
        text: colors.WHITE,
    },
    error: {
        main: colors.ERROR,
        text: colors.WHITE,
    }
};