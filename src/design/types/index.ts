interface PageViewProps {
    padding?: string | number;
};

interface ButtonProps {
    color?: string;
    textColor?: string;
    disabled?: boolean;
};

interface ButtonTextProps {
    color?: string;
};

export type { PageViewProps, ButtonProps, ButtonTextProps };